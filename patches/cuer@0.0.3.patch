diff --git a/QrCode.ts b/QrCode.ts
index 5ba0d7c..ea18c9f 100644
--- a/QrCode.ts
+++ b/QrCode.ts
@@ -10,11 +10,12 @@ export function create(value: string, options: QrCode.Options = {}): QrCode {
   const { errorCorrection, version } = options
 
-  const grid = encodeQR(value, 'raw', {
-    border: 0,
+  const encodedGrid = encodeQR(value, 'raw', {
+    border: 1,
     ecc: errorCorrection,
     scale: 1,
     version: version,
   })
+  const grid = encodedGrid.slice(1, -1).map((row) => row.slice(1, -1))
 
   const finderLength = 7
   const edgeLength = grid.length
diff --git a/_dist/QrCode.js b/_dist/QrCode.js
index acde1ec..3bbc428 100644
--- a/_dist/QrCode.js
+++ b/_dist/QrCode.js
@@ -2,11 +2,12 @@ import { encodeQR } from 'qr';
 export function create(value, options = {}) {
     const { errorCorrection, version } = options;
-    const grid = encodeQR(value, 'raw', {
-        border: 0,
+    const encodedGrid = encodeQR(value, 'raw', {
+        border: 1,
         ecc: errorCorrection,
         scale: 1,
         version: version,
     });
+    const grid = encodedGrid.slice(1, -1).map((row) => row.slice(1, -1));
     const finderLength = 7;
     const edgeLength = grid.length;
     return {
